{
  "id": "fate.enemy.guard",
  "version": "0.1.0",
  "contract_version": "0.1",
  "engine_compat": ">=0.1.0",
  "license": "MIT",
  "style": "grounded",
  "art_style": "realistic-placeholder",
  "semantic_tags": ["enemy", "guard", "combat"],
  "notes": "Guard enemy package for static or alerted grounded encounters.",
  "real_world_scale": "1 unit = 1 meter",
  "actor_class": "humanoid",
  "interaction_intent": "enemy-guard",
  "unit_system": "metric",
  "dependencies": [],
  "asset_dependencies": ["fate.assets.melee.enemy"],
  "intent_aliases": ["guard", "守卫", "看守敌人"],
  "required_capabilities": ["enemy.guard"],
  "required_slots": ["mesh.enemy"],
  "space_hints": { "footprint_meters": [0.9, 0.9, 1.9], "interaction_distance_meters": 5.0, "facing": "forward" },
  "state_hints": ["idle", "alert", "attack"],
  "constraint_hints": { "required_actor_class": "humanoid", "conflicts_with": [] },
  "capabilities": [{ "capability_id": "enemy.guard", "level": 1, "exposed_params": ["health"] }],
  "params": { "health": { "key": "health", "type": "int", "default": 140, "mutability": "runtime", "visibility": "basic", "impact": ["gameplay"] } },
  "slots": [{ "slot_id": "mesh.enemy", "slot_type": "mesh", "fallback": "builtin:enemy.guard.mesh", "optional": false, "requires": "true" }],
  "default_asset_bindings": [{ "slot_id": "mesh.enemy", "resource_type": "mesh", "asset_package_id": "fate.assets.melee.enemy", "resource_id": "guard_mesh" }],
  "state_version": "1.0.0",
  "state_migration": { "entry": "packages/enemy_guard/README.md#migration", "from_previous": "0.0.x -> 1.0.0" },
  "package_kind": "product"
}
