[package]
name = "fate-editor-shell"
version = "0.1.0"
description = "Fate editor desktop shell"
edition = "2024"

[build-dependencies]
tauri-build = { version = "2.5.6", features = [] }

[dependencies]
tauri = { version = "2.10.3", features = [] }

[features]
default = []
